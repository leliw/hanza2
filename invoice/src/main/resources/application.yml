spring:
    security:
        user:
            name: user
            password: pass
            roles: user, admin # A comma separated list of roles
    mvc:
        log-request-details: false

logging:
    level:
        org:
            springframework:
                security: INFO
        web: INFO